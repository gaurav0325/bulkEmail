# Zoho Mail SMTP Configuration for info@datanalysisninsights.co.uk
# Copy these exact values to Netlify Environment Variables

SMTP_SERVER=smtppro.zoho.com
SMTP_PORT=587
SMTP_USERNAME=info@datanalysisninsights.co.uk
SMTP_PASSWORD=your_zoho_app_password_here
FROM_EMAIL=info@datanalysisninsights.co.uk
FROM_NAME=Data Analysis Insights

# IMPORTANT SETUP STEPS:
# 1. Log into Zoho Mail (mail.zoho.com)
# 2. Go to Settings → Mail Accounts → IMAP Access → Enable IMAP
# 3. Go to Security → App Passwords → Generate App Password for "Email Client"
# 4. Use the App Password (not your regular password) in SMTP_PASSWORD above
# 5. Add these variables in Netlify Dashboard:
#    Site Settings → Environment Variables → Add each variable

# Alternative SMTP servers for Zoho (if smtppro doesn't work):
# SMTP_SERVER=smtp.zoho.com
# SMTP_SERVER=smtp.zoho.in (for India)
# SMTP_SERVER=smtp.zoho.eu (for Europe)